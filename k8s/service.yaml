apiVersion: v1
kind: Service
metadata:
  name: greetapi-service
spec:
  type: NodePort
  selector:
    app: greetapi
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
